# Server Configuration
PORT=3000

# Authentication
# Change this to a strong, random string in your .env file
JWT_SECRET=your_super_secret_key_here

# Database
# The path is relative to the backend/models directory by default
# or you can configure it via environment variables if updated in code
DATABASE_PATH=../../database/database.sqlite
