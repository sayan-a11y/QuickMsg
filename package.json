{
  "name": "quickmsg",
  "version": "2.1.0",
  "private": true,
  "scripts": {
    "start": "echo 'Run this with a static server like live-server'",
    "build": "echo 'Deploying static files'"
  }
}